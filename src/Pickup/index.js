import React from 'react';
import { AddPickupForm } from './AddPickupForm';

export const Pickup = () => {
    return (
        <div>
            <h1>Add a new Pickup</h1>
            <AddPickupForm />
        </div>
    );
};